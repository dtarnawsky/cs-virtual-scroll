import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.virtual.scroll',
  appName: 'cs-virtual-scroll',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
